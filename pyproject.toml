[project]
name = "douyin-comment-bot"
version = "0.1.0"
description = "🎵 抖音评论自动回复机器人 - 自动抓取评论并用AI生成回复"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.2",
]

[project.scripts]
douyin-comment-bot = "main:app"
